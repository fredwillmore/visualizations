= form_for @score_item do |f|
  - if @score_item.errors.any?
    #error_explanation
      %h2= "#{pluralize(@score_item.errors.count, "error")} prohibited this score_item from being saved:"
      %ul
        - @score_item.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'

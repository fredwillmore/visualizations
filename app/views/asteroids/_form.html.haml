= form_for(@asteroid) do |f|
  - if @asteroid.errors.any?
    #error_explanation
      %h2
        = pluralize(@asteroid.errors.count, "error")
        prohibited this asteroid from being saved:
      %ul
        - @asteroid.errors.full_messages.each do |message|
          %li= message
  .actions
    = f.submit

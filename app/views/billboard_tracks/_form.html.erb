<%= form_for(@billboard_track) do |f| %>
  <% if @billboard_track.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@billboard_track.errors.count, "error") %> prohibited this billboard_track from being saved:</h2>

      <ul>
      <% @billboard_track.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :string %><br>
    <%= f.text_field :string %>
  </div>
  <div class="field">
    <%= f.label :artist_id %><br>
    <%= f.text_field :artist_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
